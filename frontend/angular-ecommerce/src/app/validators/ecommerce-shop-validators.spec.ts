import { EcommerceShopValidators } from './ecommerce-shop-validators';

describe('EcommerceShopValidators', () => {
  it('should create an instance', () => {
    expect(new EcommerceShopValidators()).toBeTruthy();
  });
});
